en:
  dry_validation:
    errors:
      rules:
        password:
          filled?: "You must enter a password"
        password_confirm:
          filled?: "You must enter a confirmation password"
      password_entropy: "Your password needs to be longer or use unique characters"
      passwords_match: "Your passwords do not match"
